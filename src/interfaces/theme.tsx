export interface CustomTheme {
  dark: boolean,
  colors: {
    primary: string,
    background: string,
    card: string,
    text: string,
    border: string,
    notification: string,
    modal: string,
    pickerItem: string
  }
}